import { IconRefresh } from '@/app/components/Icons'
import React, { ReactNode } from 'react'

interface Props {
  children: ReactNode
}

function layoutGenerar({ children }: Props) {

  
  return (
    <>{children}
      
    </>

  )
}

export default layoutGenerar